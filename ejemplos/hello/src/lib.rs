// El contenido de las librerías es privado por defecto. Si se quiere que sea
// público para usarse desde fuera, hay que usar 'pub', para indicarlo.
pub fn greet() {
    println!("¡Holi gorrión!");
}
